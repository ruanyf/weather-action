name: 'GitHub Actions Weather Bot'

on:
  push:
  schedule:
    - cron: '58 1,3,5,9,11 * * *'

jobs:
  bot:
    runs-on: ubuntu-latest
    steps:
      - name: 'cron codes'
        uses: actions/checkout@v1
      - name: 'Get cron'
        run: bash ./weather.sh
